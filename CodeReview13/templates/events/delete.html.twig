{% extends 'base.html.twig' %}

{% block title %}Hello EventsController!{% endblock %}

{% block body %}
<div class="container">
    <h2 class="page-header">Delete Event {{events.name}}</h2>
    <img src="{{events.image}}" class="img-thumbnail" alt="{{events.name}}">

</div>
{% endblock %}
